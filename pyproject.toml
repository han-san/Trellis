[project]
name = "Trellis"
version = "1.0.0"
description = "A program that predicts words based on number sequences typed on keypads."
readme = "readme.md"
requires-python = ">=3.13"
dependencies = [
    "numpy<2",
    "scipy>=1.17.1",
]
